* ie7, ie8, ie9: virtualbox + win2k3srv + rdesktop
* административный интерфейс - статистика, мониторинг
* cleanup (stats, tasks, billing, accounts - see concept)
* cron: clear canceled tasks
* cron: clear deleted tests
* cron: clear finished tasks
* cron: clear deleted tests
* cron: clear history
* XPATH Browser в iframe/отдельном окне, с/без прокси (пользователь сам подгружает скрипт, Tip: Browser plugin)
* XPATH -> jQuery/CSS3 selector translator
* XMPP Reports
* SMS Reports
* Отдельный раздел и таблица notifications, каждая notification содержит параметры: email/телефон/jid. Каждый Schedule Job содержит
 набор нотификаций для успеха и сбоя. Каждый Task содержит sched_id и sched_name или null если запущен вручную. Settings как-то
 переработать - убрать галки отправки отчетов. Дефолтный набор нотификаций не делать. Возможность скопировать Schedule Job.
 Восстановление пароля и платежная информация только на E-Mail из Settings.
* php openssl_sign bug report
* php xsltprocessor recursion bug report
* доработать wine для запуска Selenium IEDriverServer
* action: label, condition (test an expression & skip to the label) - for captcha bypass
* action: save element image into var - for captcha bypass
* action: open a new window/tab or change open behavior - for captcha bypass
* action: set proxy (custom, pre-defined - вбить адреса прокси в ui а на самих прокси настроить acl, либо серые адреса через vpn)
* action selectors: xpath: & jquery: (by default xpath)
* Валидация добавляемых actions
